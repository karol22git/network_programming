#include <utility>

class host;
class Connection {
    public:
        Connection(){}
        Connect(host A, host B) {
            endpoints = std::make_pair(A, B);
        }

    private:
        std::pair<host, host> endpoints;
};